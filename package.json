{
  "name": "advice-generator",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.49.9",
    "standard": "^16.0.4",
    "vite": "^2.8.0"
  },
  "eslintConfig": {
    "extends": [
      "./node_modules/standard/eslintrc.json"
    ],
    "env": {
      "browser": true,
      "node": true
    },
    "rules": {
      "semi": [
        2,
        "always"
      ]
    }
  }
}
